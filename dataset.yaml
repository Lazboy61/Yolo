# Paths (use augmented data)
train: D:/Users/Gebruiker/Documents/Project D/Yolo/images/train_aug
val: D:/Users/Gebruiker/Documents/Project D/Yolo/images/val

# Number of classes (ONLY include classes you've labeled)
nc: 2
names:
  0: i
  1: k

# Augmentation (optimized for gestures)
augmentation:
  hsv_h: 0.01  # Minimal hue change (skin tones)
  hsv_s: 0.3    # Slight saturation variation
  hsv_v: 0.2    # Small brightness changes
  degrees: 12   # Moderate rotation range
  flipud: 0.0   # No vertical flips
  fliplr: 0.5   # 50% chance of horizontal flip (good for symmetric gestures)
  mosaic: 0.5   # Reduced to avoid weird gesture composites
  mixup: 0.1    # Very low to prevent blended gestures